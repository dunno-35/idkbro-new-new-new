kind = "web"
previewPath = "/"
title = "EventHub"
version = "1.0.0"
id = "artifacts/eventhub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21273

[services.development]
run = "pnpm --filter @workspace/eventhub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/eventhub", "run", "build" ]
publicDir = "artifacts/eventhub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21273"
BASE_PATH = "/"
